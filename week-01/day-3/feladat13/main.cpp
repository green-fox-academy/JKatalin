#include <iostream>

int main() {
    int ch = 0;
    while (ch < 100) {
        std::cout << "I won't cheat on the exam!" << std::endl;
        ch += 1;
    }

    int ch2 = 0;
    while (ch2 < 10) {
        std::cout << "I  really won't cheat on the exam!" << std::endl;
        ch2++;
    }

    int ch3 = 0;
    while (ch3 < 5) {
        std::cout << "I totally won't cheat on the exam!" << std::endl;
        ch3 += 1;
    }
    return 0;
}